numpy>=1.24
torch>=2.0
open-clip-torch>=2.24
opencv-python-headless>=4.8
Pillow>=10.0
matplotlib>=3.7
# FAISS: install only one (both expose `import faiss`). CPU works on Windows.
faiss-cpu>=1.8.0
# GPU (Linux + CUDA 12). Uninstall faiss-cpu first:
# faiss-gpu-cu12>=1.8.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-dotenv>=1.0.0
# CLIP + SigLIP2 query towers (CLIP was already imported; pin a SigLIP2-capable floor)
transformers>=4.49.0
# ASR query encode + asr_emb gallery (same MiniLM as offline embed notebooks)
sentence-transformers>=3.0.0
