# Leave this unset for local development and Cloudflare Quick Tunnel. Vite
# proxies same-origin /api requests to http://127.0.0.1:8000.
# Set it only when the API is hosted at a separate public origin.
NEXT_PUBLIC_API_BASE_URL=/api
